# Button (UI Component)

A reusable, accessible button for the frontend design system.  
Supports variants, sizes, full-width block, loading spinner, and optional icons.

---

## File

- `src/shared_UI/Button.tsx`

## Dependencies

```bash
npm install class-variance-authority clsx tailwind-merge